/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend base URL is read client-side from NEXT_PUBLIC_API_URL (default :8000).
  reactStrictMode: true,
};
module.exports = nextConfig;
